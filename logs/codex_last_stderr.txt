[precheck 1 rc=0]


[precheck 2 rc=0]


Reading prompt from stdin...
OpenAI Codex v0.121.0 (research preview)
--------
workdir: G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
model: gpt-5.4
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, C:\Users\LGe\.codex\memories]
reasoning effort: none
reasoning summaries: none
session id: 019da13d-1d18-7f93-8351-35374a142779
--------
user
작업 폴더: G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
요청: 간단한 계산기 만들어줘. 이름은 망고계산기로 해서
분석만 하지 말고 반드시 실제 파일을 생성 또는 수정해.
대답만 하지 말고 결과 파일을 작업 폴더에 남겨.
작업 완료 후 마지막에 생성/수정한 파일명을 한 줄로 적어.
codex
작업 폴더에 맞춰 현재 프로젝트 구조를 먼저 확인하고, 가장 자연스럽게 붙일 수 있는 형태로 `망고계산기`를 실제 파일로 만들겠습니다. 구현 가능한 진입 파일과 기존 스택을 확인한 뒤 바로 생성 또는 수정까지 진행하겠습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Force' in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg --files' in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
 succeeded in 571ms:


    디렉터리: G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작


Mode                 LastWriteTime         Length Name                                                                 
----                 -------------         ------ ----                                                                 
d-----        2026-04-19  오전 12:12                __pycache__                                                          
d-----        2026-04-19  오전 12:22                assets                                                               
------        2026-04-19  오전 12:12           6037 calc_app.py                                                          
------        2026-04-19  오전 12:12              4 memo 저장test.txt                                                      
------        2026-04-19  오전 12:12           6044 memo_app.py                                                          
------        2026-04-19  오전 12:12            131 generated_code.py                                                    
------        2026-04-19  오전 12:12             24 memo 불러오기test.txt                                                    
------        2026-04-19  오전 12:12           8290 notepad_app.py                                                       
------        2026-04-19  오전 12:12          10070 auto_loop_game.py                                                    
------        2026-04-19  오전 12:21          10776 mango2_notepad.html                                                  
------        2026-04-19  오전 12:22          10070 auto_loop_game.pyw                                                   
------        2026-04-19  오전 12:22           6037 calc_app.pyw                                                         
------        2026-04-19  오전 12:22            131 generated_code.pyw                                                   
------        2026-04-19  오전 12:22           6044 memo_app.pyw                                                         
------        2026-04-19  오전 12:22           8290 notepad_app.pyw                                                      
------        2026-04-19  오전 12:24          10276 망고2호기.py                                                             
------        2026-04-19  오전 12:26              3 망고2호기_메모.txt                                                         



 succeeded in 576ms:
calc_app.py
memo 저장test.txt
generated_code.py
memo_app.py
memo 불러오기test.txt
notepad_app.pyw
calc_app.pyw
memo_app.pyw
auto_loop_game.pyw
mango2_notepad.html
generated_code.pyw
망고2호기.py
망고2호기_메모.txt
notepad_app.py
auto_loop_game.py
assets\MANGO_1호기_1.jpg
assets\MANGO_1호기.jpg
assets\MANGO_1호기_2.jpg
assets\MANGO_1호기_3.jpg

codex
기존에 계산기 관련 파일이 이미 있어서, 재사용 가능한 부분을 확인한 뒤 `망고계산기` 이름으로 별도 실행 파일을 남기겠습니다. 현재 구조상 파이썬 GUI 앱으로 맞추는 게 가장 자연스럽습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'calc_app.py' -TotalCount 250" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'memo_app.py' -TotalCount 120" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
 succeeded in 445ms:
import tkinter as tk
from tkinter import filedialog, messagebox
from pathlib import Path


class MemoApp:
    def __init__(self, root: tk.Tk) -> None:
        self.root = root
        self.root.title("Simple Memo")
        self.root.geometry("900x600")

        self.current_file: Path | None = None
        self.is_modified = False

        self._build_menu()
        self._build_editor()
        self._bind_shortcuts()
        self._update_title()

    def _build_menu(self) -> None:
        menu_bar = tk.Menu(self.root)

        file_menu = tk.Menu(menu_bar, tearoff=0)
        file_menu.add_command(label="New", command=self.new_file, accelerator="Ctrl+N")
        file_menu.add_command(label="Open...", command=self.open_file, accelerator="Ctrl+O")
        file_menu.add_command(label="Save", command=self.save_file, accelerator="Ctrl+S")
        file_menu.add_command(label="Save As...", command=self.save_file_as, accelerator="Ctrl+Shift+S")
        file_menu.add_separator()
        file_menu.add_command(label="Exit", command=self.on_close)

        menu_bar.add_cascade(label="File", menu=file_menu)
        self.root.config(menu=menu_bar)

    def _build_editor(self) -> None:
        frame = tk.Frame(self.root)
        frame.pack(fill="both", expand=True)

        self.text = tk.Text(frame, undo=True, wrap="word", font=("Consolas", 12))
        scrollbar = tk.Scrollbar(frame, command=self.text.yview)
        self.text.config(yscrollcommand=scrollbar.set)

        scrollbar.pack(side="right", fill="y")
        self.text.pack(side="left", fill="both", expand=True)

        self.status_var = tk.StringVar(value="Ready")
        status_bar = tk.Label(
            self.root,
            textvariable=self.status_var,
            anchor="w",
            padx=8,
            pady=4,
            relief="sunken",
        )
        status_bar.pack(fill="x", side="bottom")

        self.text.edit_modified(False)
        self.text.bind("<<Modified>>", self._on_modified)

    def _bind_shortcuts(self) -> None:
        self.root.bind("<Control-n>", lambda event: self.new_file())
        self.root.bind("<Control-o>", lambda event: self.open_file())
        self.root.bind("<Control-s>", lambda event: self.save_file())
        self.root.bind("<Control-S>", lambda event: self.save_file_as())
        self.root.protocol("WM_DELETE_WINDOW", self.on_close)

    def _on_modified(self, _event=None) -> None:
        if self.text.edit_modified():
            self.is_modified = True
            self._update_title()
            self._update_status("Edited")
            self.text.edit_modified(False)

    def _is_dirty(self) -> bool:
        return self.is_modified

    def _update_title(self) -> None:
        name = self.current_file.name if self.current_file else "Untitled"
        dirty_prefix = "* " if self.is_modified else ""
        self.root.title(f"{dirty_prefix}{name} - Simple Memo")

    def _update_status(self, message: str) -> None:
        self.status_var.set(message)

    def _confirm_discard_changes(self) -> bool:
        if not self._is_dirty():
            return True

        answer = messagebox.askyesnocancel(
            "Unsaved Changes",
            "Save changes before continuing?",
        )
        if answer is None:
            return False
        if answer:
            return self.save_file()
        return True

    def new_file(self) -> None:
        if not self._confirm_discard_changes():
            return
        self.text.delete("1.0", "end")
        self.current_file = None
        self.is_modified = False
        self.text.edit_modified(False)
        self._update_title()
        self._update_status("New file")

    def open_file(self) -> None:
        if not self._confirm_discard_changes():
            return

        file_path = filedialog.askopenfilename(
            title="Open File",
            filetypes=[("Text Files", "*.txt"), ("Python Files", "*.py"), ("All Files", "*.*")],
        )
        if not file_path:
            return

        path = Path(file_path)
        try:

 succeeded in 455ms:
import tkinter as tk
from tkinter import messagebox


class CalculatorApp:
    def __init__(self, root: tk.Tk) -> None:
        self.root = root
        self.root.title("Tkinter Calculator")
        self.root.geometry("360x520")
        self.root.minsize(320, 460)
        self.root.configure(bg="#1f2430")

        self.expression = ""
        self.display_var = tk.StringVar(value="0")

        self._build_ui()

    def _build_ui(self) -> None:
        display_frame = tk.Frame(self.root, bg="#1f2430", padx=16, pady=20)
        display_frame.pack(fill="both")

        display = tk.Entry(
            display_frame,
            textvariable=self.display_var,
            font=("Consolas", 28),
            justify="right",
            bd=0,
            relief="flat",
            bg="#2a3142",
            fg="#f5f7fa",
            insertbackground="#f5f7fa",
        )
        display.pack(fill="both", ipady=20)

        buttons_frame = tk.Frame(self.root, bg="#1f2430", padx=16, pady=8)
        buttons_frame.pack(expand=True, fill="both")

        layout = [
            ["C", "??, "%", "/"],
            ["7", "8", "9", "*"],
            ["4", "5", "6", "-"],
            ["1", "2", "3", "+"],
            ["+/-", "0", ".", "="],
        ]

        for row_index, row in enumerate(layout):
            buttons_frame.rowconfigure(row_index, weight=1)
            for col_index, label in enumerate(row):
                buttons_frame.columnconfigure(col_index, weight=1)
                button = tk.Button(
                    buttons_frame,
                    text=label,
                    font=("Segoe UI", 18, "bold"),
                    bd=0,
                    relief="flat",
                    command=lambda value=label: self._on_button_click(value),
                    bg=self._button_bg(label),
                    fg=self._button_fg(label),
                    activebackground="#596780",
                    activeforeground="#ffffff",
                    cursor="hand2",
                )
                button.grid(
                    row=row_index,
                    column=col_index,
                    sticky="nsew",
                    padx=6,
                    pady=6,
                    ipadx=6,
                    ipady=16,
                )

        self.root.bind("<Return>", lambda _event: self._calculate())
        self.root.bind("<BackSpace>", lambda _event: self._backspace())
        self.root.bind("<Escape>", lambda _event: self._clear())

    def _button_bg(self, label: str) -> str:
        if label == "=":
            return "#ff9f1c"
        if label in {"/", "*", "-", "+", "%", "C", "??, "+/-"}:
            return "#3c4458"
        return "#2a3142"

    def _button_fg(self, label: str) -> str:
        if label == "=":
            return "#1f2430"
        return "#f5f7fa"

    def _on_button_click(self, value: str) -> None:
        if value == "C":
            self._clear()
        elif value == "??:
            self._backspace()
        elif value == "=":
            self._calculate()
        elif value == "+/-":
            self._toggle_sign()
        else:
            self._append(value)

    def _append(self, value: str) -> None:
        if self.display_var.get() == "Error":
            self._clear()

        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
            return

        if value == ".":
            current_number = self._current_number()
            if "." in current_number:
                return
            if not current_number:
                value = "0."

        self.expression += value
        self.display_var.set(self.expression or "0")

    def _current_number(self) -> str:
        parts = []
        for char in reversed(self.expression):
            if char in "+-*/%":
                break
            parts.append(char)
        return "".join(reversed(parts))

    def _clear(self) -> None:
        self.expression = ""
        self.display_var.set("0")

    def _backspace(self) -> None:
        if self.display_var.get() == "Error":
            self._clear()
            return

        self.expression = self.expression[:-1]
        self.display_var.set(self.expression or "0")

    def _toggle_sign(self) -> None:
        if not self.expression or self.display_var.get() == "Error":
            return

        operator_index = -1
        for index in range(len(self.expression) - 1, -1, -1):
            if self.expression[index] in "+-*/%":
                operator_index = index
                break

        current_number = self.expression[operator_index + 1 :]
        if not current_number:
            return

        prefix = self.expression[: operator_index + 1]
        if current_number.startswith("-"):
            current_number = current_number[1:]
        else:
            current_number = f"-{current_number}"

        self.expression = prefix + current_number
        self.display_var.set(self.expression)

    def _calculate(self) -> None:
        if not self.expression:
            return

        if self.expression[-1] in "+-*/%":
            return

        try:
            # Limit evaluation to arithmetic symbols assembled by button presses.
            result = eval(self.expression, {"__builtins__": {}}, {})
        except ZeroDivisionError:
            messagebox.showerror("Calculation Error", "0?쇰줈 ?섎닃 ???놁뒿?덈떎.")
            self.expression = ""
            self.display_var.set("Error")
            return
        except Exception:
            messagebox.showerror("Calculation Error", "怨꾩궛?앹쓣 泥섎━?????놁뒿?덈떎.")
            self.expression = ""
            self.display_var.set("Error")
            return

        if isinstance(result, float) and result.is_integer():
            result = int(result)

        self.expression = str(result)
        self.display_var.set(self.expression)


def main() -> None:
    root = tk.Tk()
    CalculatorApp(root)
    root.mainloop()


if __name__ == "__main__":
    main()

codex
기존 `calc_app.py`는 재사용 가능하지만 버튼 라벨과 메시지 일부가 깨져 있어서, 그 파일을 억지로 덮기보다 `망고계산기.py`를 새로 만들어 독립 실행되게 하겠습니다. 간단한 Tkinter GUI 계산기로 구현하고, 기본 연산과 백스페이스/부호 전환까지 넣겠습니다.
apply patch
patch: completed
G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작\망고계산기.py
diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

codex
파일 생성은 끝났고, 지금은 문법 오류 없이 실행 가능한지 바로 검증하겠습니다. 실행 테스트는 GUI를 띄우지 않고 컴파일 체크로 확인하겠습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "python -m py_compile '망고계산기.py'" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Item '망고계산기.py' | Select-Object Name,Length,LastWriteTime" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
2026-04-18T15:38:05.529188Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
Traceback (most recent call last):
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\runpy.py", line 197, in _run_module_as_main
    return _run_code(code, main_globals, None,
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\runpy.py", line 87, in _run_code
    exec(code, run_globals)
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 215, in <module>
    sys.exit(main())
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 207, in main
    compile(filename, doraise=True)
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 172, in compile
    importlib._bootstrap_external._write_atomic(cfile, bytecode, mode)
  File "<frozen importlib._bootstrap_external>", line 186, in _write_atomic
PermissionError: [Errno 13] Permission denied: '__pycache__\\망고계산기.cpython-39.pyc.35889040'

 exited 1 in 505ms:
Traceback (most recent call last):
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\runpy.py", line 197, in _run_module_as_main
    return _run_code(code, main_globals, None,
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\runpy.py", line 87, in _run_code
    exec(code, run_globals)
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 215, in <module>
    sys.exit(main())
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 207, in main
    compile(filename, doraise=True)
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\py_compile.py", line 172, in compile
    importlib._bootstrap_external._write_atomic(cfile, bytecode, mode)
  File "<frozen importlib._bootstrap_external>", line 186, in _write_atomic
PermissionError: [Errno 13] Permission denied: '__pycache__\\망고계산기.cpython-39.pyc.35889040'

 succeeded in 511ms:

Name     Length LastWriteTime         
----     ------ -------------         
망고계산기.py   6326 2026-04-19 오전 12:37:59



diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

codex
`__pycache__` 쓰기 권한 문제로 일반 컴파일 캐시 생성은 막혔습니다. 대신 바이트코드 파일을 남기지 않는 방식으로 소스 자체 문법만 다시 검증하겠습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
from pathlib import Path
source = Path('망고계산기.py').read_text(encoding='utf-8')
compile(source, '망고계산기.py', 'exec')
print('syntax_ok')
'@ | python -" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
2026-04-18T15:38:10.234802Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
Traceback (most recent call last):
  File "<stdin>", line 2, in <module>
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1266, in read_text
    with self.open(mode='r', encoding=encoding, errors=errors) as f:
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1252, in open
    return io.open(self, mode, buffering, encoding, errors, newline,
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1120, in _opener
    return self._accessor.open(self, flags, mode)
FileNotFoundError: [Errno 2] No such file or directory: '?????.py'

 exited 1 in 479ms:
Traceback (most recent call last):
  File "<stdin>", line 2, in <module>
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1266, in read_text
    with self.open(mode='r', encoding=encoding, errors=errors) as f:
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1252, in open
    return io.open(self, mode, buffering, encoding, errors, newline,
  File "C:\Users\LGe\AppData\Local\Programs\Python\Python39-32\lib\pathlib.py", line 1120, in _opener
    return self._accessor.open(self, flags, mode)
FileNotFoundError: [Errno 2] No such file or directory: '?????.py'

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$source = Get-Content -Path '"'망고계산기.py' -Raw -Encoding UTF8; @'
import sys
source = sys.stdin.read()
compile(source, 'mango_calc_source', 'exec')
print('syntax_ok')
'@ | python - <<< "'$source' in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
2026-04-18T15:38:14.349062Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.4 seconds
Output:
At line:7 char:16
+ '@ | python - <<< $source
+                ~
Missing file specification after redirection operator.
At line:7 char:15
+ '@ | python - <<< $source
+               ~
The '<' operator is reserved for future use.
At line:7 char:17
+ '@ | python - <<< $source
+                 ~
Missing file specification after redirection operator.
At line:7 char:16
+ '@ | python - <<< $source
+                ~
The '<' operator is reserved for future use.
At line:7 char:17
+ '@ | python - <<< $source
+                 ~
The '<' operator is reserved for future use.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : MissingFileSpecification
 

 exited 1 in 427ms:
At line:7 char:16
+ '@ | python - <<< $source
+                ~
Missing file specification after redirection operator.
At line:7 char:15
+ '@ | python - <<< $source
+               ~
The '<' operator is reserved for future use.
At line:7 char:17
+ '@ | python - <<< $source
+                 ~
Missing file specification after redirection operator.
At line:7 char:16
+ '@ | python - <<< $source
+                ~
The '<' operator is reserved for future use.
At line:7 char:17
+ '@ | python - <<< $source
+                 ~
The '<' operator is reserved for future use.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : MissingFileSpecification
 

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$source = Get-Content -Path '"'망고계산기.py' -Raw -Encoding UTF8; "'$source | @'"'
import sys
source = sys.stdin.read()
compile(source, 'mango_calc_source', 'exec')
print('syntax_ok')
'@ | python -" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
2026-04-18T15:38:17.814922Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.4 seconds
Output:
At line:2 char:71
+ ... urce = Get-Content -Path '망고계산기.py' -Raw -Encoding UTF8; $source | @'
+                                                                        ~~
Expressions are only allowed as the first element of a pipeline.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : ExpressionsMustBeFirstInPipeline
 

 exited 1 in 401ms:
At line:2 char:71
+ ... urce = Get-Content -Path '망고계산기.py' -Raw -Encoding UTF8; $source | @'
+                                                                        ~~
Expressions are only allowed as the first element of a pipeline.
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : ExpressionsMustBeFirstInPipeline
 

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

codex
문법 확인은 파일명을 직접 넘길 때 Windows 콘솔 인코딩 때문에 깨지고 있습니다. 파일명을 하드코딩하지 않고 디렉터리 목록에서 찾아 검증하는 방식으로 마무리하겠습니다.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
import os
for name in os.listdir('.'):
    if name.endswith('.py') and '계산기' in name:
        with open(name, 'r', encoding='utf-8') as f:
            compile(f.read(), name, 'exec')
        print('syntax_ok:', name)
        break
else:
    raise SystemExit('target file not found')
'@ | python -" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
2026-04-18T15:38:25.243903Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
target file not found

 exited 1 in 463ms:
target file not found

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
import os
for name in os.listdir('.'):
    print(repr(name))
'@ | python -" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
 succeeded in 362ms:
'__pycache__'
'calc_app.py'
'memo 저장test.txt'
'memo_app.py'
'generated_code.py'
'memo 불러오기test.txt'
'notepad_app.py'
'auto_loop_game.py'
'assets'
'mango2_notepad.html'
'auto_loop_game.pyw'
'calc_app.pyw'
'generated_code.pyw'
'memo_app.pyw'
'notepad_app.pyw'
'망고2호기.py'
'망고2호기_메모.txt'
'망고계산기.py'

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "@'
import os
TARGET = '\\ub9dd\\uace0\\uacc4\\uc0b0\\uae30.py'
with open(TARGET, 'r', encoding='utf-8') as f:
    compile(f.read(), TARGET, 'exec')
print('syntax_ok:', TARGET)
'@ | python -" in G:\내 드라이브\MANGO_v24_fix3\workspace\mango_AI_v3_시작
 succeeded in 469ms:
syntax_ok: 망고계산기.py

diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

codex
`망고계산기.py`를 작업 폴더에 새로 만들었습니다. Tkinter 기반의 간단한 GUI 계산기이며, `+`, `-`, `*`, `/`, `%`, 소수점, 부호 전환, 전체 삭제, 백스페이스, `=` 계산을 지원합니다. 창 제목과 화면 상단 이름도 `망고계산기`로 맞췄습니다.

문법 검증은 완료했습니다. 실행은 작업 폴더에서 `python 망고계산기.py`로 하면 됩니다.

망고계산기.py
diff --git a/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
new file mode 100644
index 0000000000000000000000000000000000000000..143b1ac8d3e6ad61ecdebe3521be822b77513877
--- /dev/null
+++ b/G:/내 드라이브/MANGO_v24_fix3/workspace/mango_AI_v3_시작/망고계산기.py
@@ -0,0 +1,207 @@
+import tkinter as tk
+from tkinter import messagebox
+
+
+class MangoCalculator:
+    def __init__(self, root: tk.Tk) -> None:
+        self.root = root
+        self.root.title("망고계산기")
+        self.root.geometry("360x540")
+        self.root.minsize(320, 500)
+        self.root.configure(bg="#f5b942")
+
+        self.expression = ""
+        self.display_var = tk.StringVar(value="0")
+
+        self._build_ui()
+
+    def _build_ui(self) -> None:
+        header = tk.Frame(self.root, bg="#f5b942", padx=16, pady=16)
+        header.pack(fill="x")
+
+        title = tk.Label(
+            header,
+            text="망고계산기",
+            font=("Malgun Gothic", 18, "bold"),
+            bg="#f5b942",
+            fg="#4a2d00",
+            anchor="w",
+        )
+        title.pack(fill="x")
+
+        display = tk.Entry(
+            self.root,
+            textvariable=self.display_var,
+            font=("Consolas", 28),
+            justify="right",
+            bd=0,
+            relief="flat",
+            bg="#fff6dd",
+            fg="#332100",
+            insertbackground="#332100",
+        )
+        display.pack(fill="x", padx=16, pady=(0, 12), ipady=18)
+
+        buttons_frame = tk.Frame(self.root, bg="#f5b942", padx=16, pady=8)
+        buttons_frame.pack(expand=True, fill="both")
+
+        layout = [
+            ["C", "←", "%", "/"],
+            ["7", "8", "9", "*"],
+            ["4", "5", "6", "-"],
+            ["1", "2", "3", "+"],
+            ["+/-", "0", ".", "="],
+        ]
+
+        for row_index, row in enumerate(layout):
+            buttons_frame.rowconfigure(row_index, weight=1)
+            for col_index, label in enumerate(row):
+                buttons_frame.columnconfigure(col_index, weight=1)
+                button = tk.Button(
+                    buttons_frame,
+                    text=label,
+                    font=("Malgun Gothic", 16, "bold"),
+                    bd=0,
+                    relief="flat",
+                    cursor="hand2",
+                    bg=self._button_bg(label),
+                    fg=self._button_fg(label),
+                    activebackground="#ffcf73",
+                    activeforeground="#332100",
+                    command=lambda value=label: self._on_button_click(value),
+                )
+                button.grid(
+                    row=row_index,
+                    column=col_index,
+                    sticky="nsew",
+                    padx=6,
+                    pady=6,
+                    ipadx=6,
+                    ipady=16,
+                )
+
+        self.root.bind("<Return>", lambda _event: self._calculate())
+        self.root.bind("<BackSpace>", lambda _event: self._backspace())
+        self.root.bind("<Escape>", lambda _event: self._clear())
+
+    def _button_bg(self, label: str) -> str:
+        if label == "=":
+            return "#ff8a00"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#7a4b00"
+        return "#fff0c4"
+
+    def _button_fg(self, label: str) -> str:
+        if label == "=":
+            return "#ffffff"
+        if label in {"/", "*", "-", "+", "%", "C", "←", "+/-"}:
+            return "#fff8ea"
+        return "#332100"
+
+    def _on_button_click(self, value: str) -> None:
+        if value == "C":
+            self._clear()
+            return
+        if value == "←":
+            self._backspace()
+            return
+        if value == "=":
+            self._calculate()
+            return
+        if value == "+/-":
+            self._toggle_sign()
+            return
+        self._append(value)
+
+    def _append(self, value: str) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+
+        if value in "+-*/%" and (not self.expression or self.expression[-1] in "+-*/%"):
+            return
+
+        if value == ".":
+            current_number = self._current_number()
+            if "." in current_number:
+                return
+            if not current_number:
+                value = "0."
+
+        self.expression += value
+        self.display_var.set(self.expression or "0")
+
+    def _current_number(self) -> str:
+        parts: list[str] = []
+        for char in reversed(self.expression):
+            if char in "+-*/%":
+                break
+            parts.append(char)
+        return "".join(reversed(parts))
+
+    def _clear(self) -> None:
+        self.expression = ""
+        self.display_var.set("0")
+
+    def _backspace(self) -> None:
+        if self.display_var.get() == "오류":
+            self._clear()
+            return
+
+        self.expression = self.expression[:-1]
+        self.display_var.set(self.expression or "0")
+
+    def _toggle_sign(self) -> None:
+        if not self.expression or self.display_var.get() == "오류":
+            return
+
+        operator_index = -1
+        for index in range(len(self.expression) - 1, -1, -1):
+            if self.expression[index] in "+-*/%":
+                operator_index = index
+                break
+
+        current_number = self.expression[operator_index + 1 :]
+        if not current_number:
+            return
+
+        prefix = self.expression[: operator_index + 1]
+        if current_number.startswith("-"):
+            current_number = current_number[1:]
+        else:
+            current_number = f"-{current_number}"
+
+        self.expression = prefix + current_number
+        self.display_var.set(self.expression)
+
+    def _calculate(self) -> None:
+        if not self.expression or self.expression[-1] in "+-*/%":
+            return
+
+        try:
+            result = eval(self.expression, {"__builtins__": {}}, {})
+        except ZeroDivisionError:
+            messagebox.showerror("계산 오류", "0으로 나눌 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+        except Exception:
+            messagebox.showerror("계산 오류", "식을 계산할 수 없습니다.")
+            self.expression = ""
+            self.display_var.set("오류")
+            return
+
+        if isinstance(result, float) and result.is_integer():
+            result = int(result)
+
+        self.expression = str(result)
+        self.display_var.set(self.expression)
+
+
+def main() -> None:
+    root = tk.Tk()
+    MangoCalculator(root)
+    root.mainloop()
+
+
+if __name__ == "__main__":
+    main()

tokens used
10,706